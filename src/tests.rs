/// Tests the whole pq library
/// Run with: `cargo test --lib pq`

#[test]
fn test_pq_lib_api()
{
    assert_eq!(
        "this should test src/main.rs or src/lib.rs, if --lib/--bin are passed",
        "this should test src/main.rs or src/lib.rs, if --lib/--bin are passed"
    );
}
