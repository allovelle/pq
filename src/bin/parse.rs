use pq::ast::ParseResult;
use pq::tok::Tok;

fn main() -> ParseResult<()>
{
    /*  pq::ast::build_ast_table(&[
        Tok::ArrayOpen,
        Tok::Number(818.0),
        Tok::ArrayClose,
    ])?; */

    Ok(())
}
